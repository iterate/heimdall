(ns eu.teod.heimdall.main)

;; Production entrypoint

;; Load env var conf (if present), merge with our default conf, and go.

;; What do we care about? Both the handler and the server? Dunno. Perhaps.

(defn -main [& args])
